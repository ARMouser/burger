<h1> Eat-da-burger! </h1>

<h2>Ready to Eat! </h2>
<ul>
    {{#each burgers}}
     {{#unless this.devoured}}
    <li>
        <h3>{{this.burger_name}}</h3>
    </li>
     {{/unless}}
    {{/each}}
</ul>

<h2>Burgers people loved </h2>

{{!--<ul>
    {{#each burgers}}
        {{#if this.devoured}}
        <li>
            <h3>{{this.burger_name}}</h3>
        </li>
    {{/each}}
</ul>--}}

<h2>Make your own burger!</h2>
<form method='post' action='/'>
    <h3>Burger Name:</h3>
    <input type='text' class='burger_name'>
    <h3>Toppings</h3>
    <input type='text' class='toppings_'>
    <button class='submit'>Submit</button>
</form>